<style>
    #submitPayment{
        background:aqua;
        width:100%;
        font-size:18px;
        padding:10px;
    }
</style>
<script src="/js/jquery-2.1.3.min.js"></script>
<form action="https://www.sandbox.paypal.com/cgi-bin/webscr" method="post" target="_top">
    <?= $form->getElement('cmd')->renderElement(); ?>
    <?= $form->getElement('business')->renderElement(); ?>
    <?= $form->getElement('item_name')->renderElement(); ?>
    <?= $form->getElement('amount')->renderElement(); ?>
    <?= $form->getElement('currency_code')->renderElement(); ?>
    <?= $form->getElement('quantity')->renderElement(); ?>
    <?= $form->getElement('rm')->renderElement(); ?>
    <?= $form->getElement('return')->renderElement(); ?>
    <?= $form->getElement('custom')->renderElement(); ?>
    <button id='submitPayment' type='submit' class='btn btn-primary'>Processing to payment provider... Please wait</button>
</form>

<script>
    $(document).ready(function(){
       $('#submitPayment').click(); 
    });
</script>