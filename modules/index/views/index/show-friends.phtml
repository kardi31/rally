<?php if(isset($friends)): ?>
    <ul>
    <?php foreach($friends as $friend): ?>
        <li>
            <?php 
            if($friend['user_id']==$user['id']){
                if(strtotime($friend['UserFriends']['last_active'])>strtotime("-2 minutes")){
                    echo '<img src="/images/layout/bulb-on.png" />';
                }
                else{
                    echo '<img src="/images/layout/bulb.png" />';
                }

                echo $friend['UserFriends']['username']; 
            }
            else{
                if(strtotime($friend['User']['last_active'])>strtotime("-2 minutes")){
                    echo '<img src="/images/layout/bulb-on.png" />';
                }
                else{
                    echo '<img src="/images/layout/bulb.png" />';
                }

                echo $friend['User']['username']; 
            }
            ?>
        </li>

    <?php endforeach; ?>
    </ul>
<?php endif; ?>